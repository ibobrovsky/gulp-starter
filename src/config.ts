import { AppConfig } from '../core/config'

const config: AppConfig = {
  stylesMixins: ['@assets/styles/variables/index.scss'],
  globalStyles: ['normalize.css/normalize.css', '@assets/styles/index.scss'],
}

export default config
