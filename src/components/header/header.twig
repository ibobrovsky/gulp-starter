{% macro header(props = {}) %}
    <header class="header">
        <div class="header__container">
            {{ props.global.header.items | json_encode }}
        </div>
    </header>
{% endmacro %}