import { Deps } from '../../../core/types'

const deps: Deps = {
  links: [
    {
      uid: 'sprite',
      rel: 'preload',
      href: './symbols/sprite.svg',
      as: 'image',
      type: 'image/svg+xml',
    },
  ],
}

export default deps
